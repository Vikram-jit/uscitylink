import 'package:flutter/material.dart';

class WebVierw extends StatelessWidget {
  const WebVierw({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
