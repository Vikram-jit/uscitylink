<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Training Certificate</title>

    <!-- Import Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&family=Great+Vibes&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        margin: 0;
        padding: 0;
        position: relative;
        text-align: center;
      }

      .certificate-container {
        position: relative;
        display: inline-block;
      }

      .certificate-image {
        width: 100%;
        height: auto;
        display: block;
      }

      .heading {
        position: absolute;
        top: 17%; /* Adjust this value for precise positioning */
        left: 50%;
        transform: translate(-50%, 0); /* Keep horizontal centering */
        font-family: "Great Vibes", serif;
        font-weight: 900;
        font-size: 80px;
        color: #000; /* Adjust color based on your image */
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2); /* Optional for better visibility */
      }

      .title {
        position: absolute;
        top: 34%; /* Adjust vertical positioning */
        left: 50%;
        transform: translate(-50%, 0);

        font-size: 36px; /* Adjust as needed */
        font-weight: bold;
        color: #000;
        text-align: center;
        width: 60%; /* Manage text length */
        max-width: 600px; /* Prevents overflow */
        word-wrap: break-word;
        white-space: normal;
        font-family: "Cormorant Garamond", serif;
      }
      .date {
        position: absolute;
        top: 40%; /* Adjust vertical positioning */
        left: 50%;
        transform: translate(-50%, 0);

        font-size: 32px; /* Adjust as needed */
        font-weight: bold;
        color: #000;
        text-align: center;
        width: 60%; /* Manage text length */
        max-width: 600px; /* Prevents overflow */
        word-wrap: break-word;
        white-space: normal;
        font-family: "Cormorant Garamond", serif;
      }

      .award {
        position: absolute;
        top: 50%; /* Adjust vertical positioning */
        left: 50%;
        transform: translate(-50%, 0);

        font-size: 32px; /* Adjust as needed */
        font-weight: bold;
        color: #000;
        text-align: center;
        width: 60%; /* Manage text length */
        max-width: 600px; /* Prevents overflow */
        word-wrap: break-word;
        white-space: normal;
        font-family: "Cormorant Garamond", serif;
        text-decoration: underline;
      }

      .name {
        position: absolute;
        top: 56%; /* Adjust vertical positioning */
        left: 50%;
        transform: translate(-50%, 0);

        font-size: 56px; /* Adjust as needed */
        font-weight: bold;
        color: #000;
        text-align: center;
        width: 60%; /* Manage text length */
        max-width: 600px; /* Prevents overflow */
        word-wrap: break-word;
        white-space: normal;
        font-family: "Great Vibes", serif;
      }
    </style>
  </head>
  <body>
    <div class="certificate-container">
      <img
        class="certificate-image"
        src="http://localhost:4300/c5.jpg"
        alt="Certificate Background"
      />
      <div class="heading">Certificate of Training</div>

      <div class="title"><%= title %></div>
      <div class="date">Completion Date: <%= date %></div>
      <div class="award">Awarded To</div>

      <div class="name"><%= name %></div>
    </div>
  </body>
</html>
