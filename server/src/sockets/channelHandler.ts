import { Server } from "socket.io";
import UserChannel from "../models/UserChannel";
import { CustomSocket } from "./socket";

export async function addMemberToChannel(io:Server,socket:CustomSocket, channel:UserChannel) {

}   