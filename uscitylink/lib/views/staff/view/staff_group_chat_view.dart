import 'package:flutter/widgets.dart';

class StaffGroupChatView extends StatelessWidget {
  const StaffGroupChatView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
